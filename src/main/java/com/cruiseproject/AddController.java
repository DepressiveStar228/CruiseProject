package com.cruiseproject;

public class AddController {

    private static void setCruises(){

    }


    /*public static void addTicket(){
        Stage window = new Stage();
        window.initModality(Modality.APPLICATION_MODAL);
        Pane pane = new Pane();
        Label labelCruise = new Label("Виберіть круїз");
        TextField textFieldCruise = new TextField();

        labelCruise.relocate(50, 60);
        textFieldCruise.relocate(50, 80);
        pane.getChildren().addAll(labelCruise, textFieldCruise);

        Scene scene = new Scene(pane, 400, 300);
        window.setScene(scene);
        window.setTitle("Додати значення до Array List");
        window.showAndWait();
    } */
}
